# push.sh — local credentials
#
# Copy this to .env and fill in real values:
#   cp scripts/.env.template scripts/.env
#
# scripts/.env is gitignored and must never be committed.

LARA_API_KEY=your_deploy_api_key_here
